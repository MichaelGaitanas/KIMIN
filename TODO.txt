- Add global Cartesian frame of reference (plain RGB GL_LINES centered at the world origin).
- Assume that the 3rd body (orbiter) can also be a rigid body (not only a point mass).
- Implement cascaded shadow map algorithm.
- Add SRP to asteroids (cannonball & faceted model).
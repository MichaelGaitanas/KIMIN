- Add global Cartesian frame of reference (plain RGB GL_LINES centered at the world origin).
- Assume that the 3rd body (orbiter) can be a rigid body instead of a point mass (rotational dynamics).
- Fix shadow algorithm (cascaded shadow).
- Render the Sun better (glowing rays).
- Add SRP to asteroids and 3rd body (cannonball & faceted model).
- Add non-inertial terms for the barycentric frame.
- Add binary's revolution around the Sun.